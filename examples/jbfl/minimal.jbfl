/* PadDecimals gives node coordinates a uniform number of decimals. It reads
  the source text to do that, so 12.0 keeps its point and 0.12000 is trimmed
  back, and PreserveNumberFormat is not needed alongside it. */
.*.nodes[*][*] {
    PadDecimals: 3;
}

.*.nodes, .*.beams,
.*.triangles {
    AutoPad: true;
}

.*.nodes {
    AlignObjectKeys: true;
}

.*.beams[*], .*.triangles[*],
.*.variables[*], .*.glowMap.*,
.*.rails.*, .*.props[*],
.*.slots[*], .*.slots2[*],
.*.controller[*],
.*.components.electrics.smoothers[*],
.*.mainEngine.torque[*],
.*.mainEngine.burnEfficiency[*],
.*.mainEngine.torqueModIntake[*],
.*.mainEngine.torqueModMult[*],
.*.powertrain[*], .*.flexbodies[*] {
    PreserveNumberFormat : true;
    ComplexNewLine : None;
}

.*.nodes[*] {
    ComplexNewLine : None;
}

.*.nodes[*].*, .*.nodes[*][*].*,
.*.beams[*].*, .*.beams[*][*].*,
.*.slots[*].*, .*.slots[*][*].*,
.*.slots2[*].*, .*.slots2[*][*].*,
.*.props[*].*, .*.props[*][*].*,
.*.flexbodies[*].*,
.*.flexbodies[*][*].* {
    PreserveNumberFormat : true;
    ComplexNewLine : None;
}

.*.mainEngine, .*.sounds,
.*.soundConfig, .*.soundConfigExhaust,
.*.information {
    ComplexNewLine: Force;
    AlignObjectKeys: true;
    PreserveNumberFormat: true;
}

.*.glowMap {
    AlignObjectKeys: true;
    AutoPadSubObjects: true;
}
